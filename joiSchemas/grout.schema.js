const joi = require('@hapi/joi');

// exports.createGroutSchema = joi.object({

//     groutName: joi.string().alphanum().min(6).max(45).required(),

// });

// exports.updateGroutSchema = joi.object({
//     groutName: joi.string().regex(/^[a-zA-Z0-9 ]*$/).min(6).max(45).optional(),
//     image:joi.string().optional
// });